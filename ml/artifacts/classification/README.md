# Classification Model Artifacts

This directory stores trained classification model files:

- `vectorizer.joblib` — TF-IDF vectorizer
- `classifier.joblib` — Logistic Regression classifier
- `metadata.json` — Training metadata and evaluation metrics

These files are generated by the training script and should NOT be committed to git.
